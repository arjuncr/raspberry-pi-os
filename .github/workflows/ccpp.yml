name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Running a multi-line script
      run: |
         ./build_rpi_os.sh --build-all
